# Screenshots

Add dashboard and widget screenshots/GIFs here and reference them from the root
README (placeholders are marked with `TODO` there).

Suggested captures:

- `dashboard.png` — the full dashboard: replay stage, leaderboard, next-race card.
- `widget.png` / `widget.gif` — the compact autoplay `<RaceReplayWidget>`.
